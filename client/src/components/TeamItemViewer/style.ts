import styled from 'styled-components';

const ThreeDot = styled.img`
  padding: 0px 10px;
`;

const SearchBarContainer = styled.div`
  display: flex;
  flex-direction: row-reverse;
  margin: 10px 0px;
`;

export { ThreeDot, SearchBarContainer };
