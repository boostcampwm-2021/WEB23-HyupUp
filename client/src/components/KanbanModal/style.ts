import styled from 'styled-components';

const Styled = {
  Wrapper: styled.section`
    div {
      height: 800px;
      width: 800px;
      overflow-y: scroll;
    }
  `,
};

export default Styled;
